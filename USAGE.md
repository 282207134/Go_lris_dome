# Iris Go 框架学习项目使用指南

## 📋 目录

1. [项目概述](#项目概述)
2. [快速开始](#快速开始)
3. [API 接口文档](#api-接口文档)
4. [项目结构说明](#项目结构说明)
5. [开发指南](#开发指南)
6. [部署指南](#部署指南)
7. [常见问题](#常见问题)
8. [学习路径](#学习路径)

## 🎯 项目概述

这是一个完整的 Iris Go Web 框架学习项目，包含了现代 Web 开发的各种最佳实践和技术栈。项目采用分层架构，代码结构清晰，注释详细，特别适合 Go 语言学习者。

### 主要特性

- ✅ **完整的 Web 框架**: 基于 Iris v12 构建
- ✅ **RESTful API**: 标准的 REST API 设计
- ✅ **JWT 身份验证**: 完整的认证授权机制
- ✅ **数据库集成**: GORM + SQLite
- ✅ **数据验证**: 完整的输入验证
- ✅ **错误处理**: 统一的错误处理机制
- ✅ **中间件系统**: 丰富的中间件示例
- ✅ **模板引擎**: HTML 模板渲染
- ✅ **静态文件服务**: 文件上传下载
- ✅ **中文文档**: 详细的中文注释和文档

## 🚀 快速开始

### 环境要求

- Go 1.21 或更高版本
- Git
- 可选：Docker 和 Docker Compose

### 安装步骤

1. **克隆项目**
```bash
git clone <项目地址>
cd iris-cn-sample-project
```

2. **安装依赖**
```bash
go mod download
```

3. **运行项目**
```bash
# 方式一：直接运行
go run main.go

# 方式二：使用启动脚本
chmod +x start.sh
./start.sh

# Windows 用户
start.bat

# 方式三：使用 Makefile
make run

# 方式四：开发模式（需要 air）
make dev
```

4. **访问应用**
- 主页: http://localhost:8080
- API 文档: http://localhost:8080/api/docs
- 健康检查: http://localhost:8080/api/health

### Docker 运行

1. **构建镜像**
```bash
docker build -t iris-sample .
```

2. **运行容器**
```bash
docker run -p 8080:8080 iris-sample
```

3. **使用 Docker Compose**
```bash
docker-compose up -d
```

## 📚 API 接口文档

### 认证相关

#### 用户登录
```bash
curl -X POST "http://localhost:8080/api/auth/login" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "admin123"
  }'
```

#### 用户注册
```bash
curl -X POST "http://localhost:8080/api/auth/register" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "newuser",
    "email": "newuser@example.com",
    "password": "password123",
    "first_name": "张",
    "last_name": "三"
  }'
```

#### 刷新令牌
```bash
curl -X POST "http://localhost:8080/api/auth/refresh" \
  -H "Authorization: Bearer <your-refresh-token>"
```

### 用户管理

#### 获取用户列表（需要认证）
```bash
curl -X GET "http://localhost:8080/api/users" \
  -H "Authorization: Bearer <your-jwt-token>"
```

#### 获取用户详情
```bash
curl -X GET "http://localhost:8080/api/users/1" \
  -H "Authorization: Bearer <your-jwt-token>"
```

### 示例接口

#### 简单问候
```bash
curl "http://localhost:8080/api/hello?name=世界"
```

#### 路径参数示例
```bash
curl "http://localhost:8080/api/data/123"
```

#### 表单数据处理
```bash
curl -X POST "http://localhost:8080/api/form" \
  -F "name=张三" \
  -F "email=zhangsan@example.com" \
  -F "age=25"
```

#### 文件上传
```bash
curl -X POST "http://localhost:8080/api/upload" \
  -F "file=@/path/to/your/file.jpg"
```

## 🏗️ 项目结构说明

```
iris-cn-sample-project/
├── main.go                 # 应用程序入口
├── go.mod                  # Go 模块定义文件
├── go.sum                  # 依赖版本锁定文件
├── README.md               # 项目说明文档
├── Makefile               # 构建脚本
├── Dockerfile             # Docker 构建文件
├── docker-compose.yml      # Docker Compose 配置
├── start.sh               # Linux/Mac 启动脚本
├── start.bat              # Windows 启动脚本
├── docs/                  # 详细文档目录
│   ├── 00-项目总结.md
│   ├── 01-基础入门.md
│   ├── 02-路由系统.md
│   ├── 03-中间件使用.md
│   ├── 04-请求处理.md
│   ├── 05-响应处理.md
│   ├── 06-静态文件.md
│   ├── 07-模板引擎.md
│   ├── 08-数据库集成.md
│   ├── 09-身份验证.md
│   └── 10-错误处理.md
├── config/                # 配置文件目录
│   └── config.go          # 应用配置管理
├── controllers/           # 控制器目录
│   ├── user_controller.go # 用户相关控制器
│   ├── auth_controller.go # 认证相关控制器
│   └── api_controller.go  # API 示例控制器
├── middleware/            # 中间件目录
│   ├── auth.go            # 认证中间件
│   ├── cors.go            # 跨域中间件
│   ├── logger.go          # 日志中间件
│   └── recovery.go        # 恢复中间件
├── models/                # 数据模型目录
│   ├── user.go            # 用户模型
│   └── response.go        # 响应模型
├── services/              # 服务层目录
│   ├── user_service.go    # 用户服务
│   └── auth_service.go    # 认证服务
├── utils/                 # 工具函数目录
│   ├── jwt.go             # JWT 工具
│   ├── validator.go       # 验证工具
│   └── response.go        # 响应工具
├── static/                # 静态文件目录
│   ├── css/               # 样式文件
│   ├── js/                # JavaScript 文件
│   └── images/            # 图片文件
├── templates/             # 模板文件目录
│   ├── layouts/           # 布局模板
│   ├── index.html         # 首页模板
│   ├── users.html         # 用户列表模板
│   ├── user.html          # 用户详情模板
│   └── error.html         # 错误页面模板
└── database/              # 数据库目录
    └── database.go        # 数据库初始化
```

## 💻 开发指南

### 开发环境设置

1. **安装开发工具**
```bash
make install-tools
```

2. **代码格式化**
```bash
make fmt
```

3. **代码检查**
```bash
make lint
```

4. **运行测试**
```bash
make test
```

5. **热重载开发**
```bash
make dev
```

### 代码规范

1. **命名规范**
- 包名：小写，简洁
- 变量名：驼峰命名
- 常量名：大写，下划线分隔
- 函数名：驼峰命名，导出函数首字母大写

2. **注释规范**
- 所有公共函数必须有注释
- 复杂逻辑需要行内注释
- 注释使用中文，便于理解

3. **错误处理**
- 使用统一的错误类型
- 记录详细的错误信息
- 提供用户友好的错误消息

### 添加新功能

1. **创建模型**
```go
// models/new_model.go
type NewModel struct {
    BaseModel
    Name string `json:"name" gorm:"not null;size:100"`
    // 其他字段
}
```

2. **创建服务**
```go
// services/new_service.go
type NewService struct {
    db *gorm.DB
}

func NewNewService() *NewService {
    return &NewService{db: database.GetDB()}
}
```

3. **创建控制器**
```go
// controllers/new_controller.go
func CreateNew(ctx iris.Context) {
    // 处理逻辑
}
```

4. **添加路由**
```go
// main.go 中添加路由
app.Get("/api/new", CreateNew)
```

## 🚀 部署指南

### 生产环境部署

1. **构建应用**
```bash
make build-prod
```

2. **使用 Docker**
```bash
docker build -t iris-sample:latest .
docker run -d -p 8080:8080 \
  -e GIN_MODE=release \
  -e JWT_SECRET=your-production-secret \
  iris-sample:latest
```

3. **使用 Docker Compose**
```bash
docker-compose -f docker-compose.prod.yml up -d
```

### 环境变量配置

生产环境需要配置以下环境变量：

```bash
# 服务器配置
GIN_MODE=release
SERVER_PORT=8080

# 数据库配置
DB_DRIVER=sqlite
DB_DATABASE=/app/data/iris_sample.db

# JWT 配置
JWT_SECRET=your-very-secure-secret-key
JWT_EXPIRATION_TIME=86400
JWT_ISSUER=iris-sample-project

# 日志配置
LOG_LEVEL=info
LOG_FORMAT=json
```

### 反向代理配置

#### Nginx 配置示例
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /static/ {
        alias /path/to/static/;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

## ❓ 常见问题

### 1. 依赖安装失败

**问题**: `go mod download` 失败

**解决方案**:
```bash
# 设置 Go 代理
go env -w GOPROXY=https://goproxy.cn,direct
go env -w GOSUMDB=sum.golang.google.cn

# 清理模块缓存
go clean -modcache

# 重新下载依赖
go mod download
```

### 2. 数据库连接失败

**问题**: SQLite 数据库连接失败

**解决方案**:
- 确保数据目录存在且有写权限
- 检查数据库文件路径是否正确
- 查看错误日志获取详细信息

### 3. JWT 令牌无效

**问题**: JWT 令牌验证失败

**解决方案**:
- 检查 JWT_SECRET 配置是否正确
- 确认令牌未过期
- 验证令牌格式是否正确（Bearer <token>）

### 4. 跨域问题

**问题**: 前端无法访问 API

**解决方案**:
- 确保 CORS 中间件已正确配置
- 检查请求头是否包含必要的 CORS 信息
- 验证预检请求是否正确处理

### 5. 模板渲染失败

**问题**: HTML 模板无法渲染

**解决方案**:
- 检查模板文件路径是否正确
- 验证模板语法是否正确
- 确认模板引擎已正确注册

## 📖 学习路径

建议按照以下顺序学习本项目：

### 第一阶段：基础概念（1-2天）
1. 阅读 [01-基础入门.md](./docs/01-基础入门.md)
2. 理解项目结构和设计理念
3. 运行项目，熟悉基本功能

### 第二阶段：核心功能（3-5天）
1. 学习 [02-路由系统.md](./docs/02-路由系统.md)
2. 掌握 [03-中间件使用.md](./docs/03-中间件使用.md)
3. 理解 [04-请求处理.md](./docs/04-请求处理.md)
4. 学习 [05-响应处理.md](./docs/05-响应处理.md)

### 第三阶段：进阶功能（3-5天）
1. 掌握 [06-静态文件.md](./docs/06-静态文件.md)
2. 学习 [07-模板引擎.md](./docs/07-模板引擎.md)
3. 理解 [08-数据库集成.md](./docs/08-数据库集成.md)
4. 掌握 [09-身份验证.md](./docs/09-身份验证.md)

### 第四阶段：高级特性（2-3天）
1. 学习 [10-错误处理.md](./docs/10-错误处理.md)
2. 理解项目架构和设计模式
3. 学习性能优化技巧

### 第五阶段：实践项目（5-7天）
1. 基于项目模板开发自己的应用
2. 添加新功能和特性
3. 进行代码重构和优化
4. 部署到生产环境

## 📞 技术支持

如果在学习过程中遇到问题，可以通过以下方式获取帮助：

1. **查看文档**: 首先查看项目文档和代码注释
2. **搜索问题**: 搜索相关的错误信息和解决方案
3. **提交 Issue**: 在项目仓库提交问题
4. **社区讨论**: 参与相关技术社区的讨论

## 🎉 总结

这个 Iris Go 框架学习项目为您提供了一个完整的学习资源，涵盖了现代 Web 开发的各个方面。通过学习这个项目，您将：

- 掌握 Iris 框架的核心概念和用法
- 理解 Go 语言 Web 开发的最佳实践
- 学会构建完整的 Web 应用程序
- 具备独立开发项目的能力

祝您学习愉快！🚀